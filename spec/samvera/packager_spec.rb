require "spec_helper"

RSpec.describe Samvera::Packager do
  it "has a version number" do
    expect(Samvera::Packager::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
